# Config templates

The config files in this directory are used as templates for configuration
that is created for finders.

Drupal core does not use this directory.

@see \Drupal\finders\FinderConfigManager
